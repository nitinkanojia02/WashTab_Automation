{
  "pageName": "login_page",
  "keywords": [
    {
      "keywordId": "KW_001",
      "keywordName": "Click Close Button",
      "targetElement": "close_button",
      "action": "click",
      "arguments": [],
      "implementation": [
        "    [Documentation]    Click action for Close Button (button) using page object locator.",
        "    Wait Until Element Is Visible    ${CLOSE_BUTTON}    10s",
        "    Click Element    ${CLOSE_BUTTON}"
      ],
      "approved": true
    },
    {
      "keywordId": "KW_002",
      "keywordName": "Enter Ion Input",
      "targetElement": "ion_input",
      "action": "input",
      "arguments": [
        "password"
      ],
      "implementation": [
        "    [Documentation]    Enter password action for Ion Input (password) using page object locator.",
        "    Wait Until Element Is Visible    ${PASSWORD}    10s",
        "    Input Password    ${PASSWORD}    ${password}"
      ],
      "approved": true
    },
    {
      "keywordId": "KW_003",
      "keywordName": "Enter Ion Input 1",
      "targetElement": "ion_input_1",
      "action": "input",
      "arguments": [
        "password"
      ],
      "implementation": [
        "    [Documentation]    Enter password action for Ion Input 1 (password) using page object locator.",
        "    Wait Until Element Is Visible    ${ION_INPUT_1_PASSWORD}    10s",
        "    Input Password    ${ION_INPUT_1_PASSWORD}    ${password}"
      ],
      "approved": true
    }
  ]
}